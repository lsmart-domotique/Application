import { StyleSheet } from 'react-native';
import platform from '../../native-base-theme/variables/lsmart';

export const input = StyleSheet.create({
  default: {
    fontSize: platform.DefaultFontSize,
    paddingLeft: 0,
  },
});
